# Cognito User Creation Configuration
# Copy this file to .env.users and fill in your values

# Users to create (comma-separated emails) - REQUIRED
USERS=user1@example.com,user2@example.com,admin@example.com

# Optional: AWS Region (auto-fetched from Terraform if not set)
# AWS_REGION=us-east-1

# Optional: Cognito User Pool ID (auto-fetched from Terraform if not set)
# COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx

# Optional: Set temporary password (will be auto-generated if not set) password will be showin in the terminal
# TEMP_PASSWORD=TempPass123!
